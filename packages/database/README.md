# Database boundary

This package owns server-side database client creation and generated database types. Milestone 0 intentionally contains no domain tables. Create future migrations with `pnpm supabase migration new <name>`, reset locally with `pnpm db:reset`, and regenerate types with `pnpm db:types`.
